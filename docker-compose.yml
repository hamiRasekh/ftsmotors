services:
  postgres:
    image: postgres:15-alpine
    container_name: ftsmotors-db
    environment:
      POSTGRES_USER: ftsmotors
      POSTGRES_PASSWORD: ftsmotors123
      POSTGRES_DB: ftsmotors
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ftsmotors"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - ftsmotors-network

  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile.compose
    container_name: ftsmotors-backend
    environment:
      DATABASE_URL: postgresql://ftsmotors:ftsmotors123@postgres:5432/ftsmotors?schema=public
      JWT_SECRET: your-super-secret-jwt-key-change-in-production
      JWT_EXPIRES_IN: 7d
      PORT: 4000
      NODE_ENV: production
      FRONTEND_URL: http://localhost:3000
    ports:
      - "4000:4000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./apps/backend:/app/apps/backend
      - ./packages/shared:/app/packages/shared
      - /app/node_modules
      - /app/packages/shared/node_modules
    networks:
      - ftsmotors-network

  frontend:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile.compose
    container_name: ftsmotors-frontend
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:4000
      NEXT_PUBLIC_SITE_URL: http://localhost:3000
    ports:
      - "3000:3000"
    depends_on:
      - backend
    volumes:
      - ./apps/frontend:/app/apps/frontend
      - ./packages/shared:/app/packages/shared
      - /app/node_modules
      - /app/packages/shared/node_modules
      - /app/apps/frontend/.next
      - /app/apps/frontend/node_modules
    networks:
      - ftsmotors-network

networks:
  ftsmotors-network:
    driver: bridge

volumes:
  postgres_data:
